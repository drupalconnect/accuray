/**
 * @file
 * Panels Styling
 */

/**
 * Generic styles
 */ 
div.panel-pane, div.node {
    margin: inherit;
    padding: inherit;
}

.panels-flexible-row {
  border-radius: 3px;
  -moz-border-radius: 3px;
  -webkit-border-radius: 3px;
}

.panels-flexible-row .inside {
  padding: 0;
  margin: 0;
  border-radius: 3px;
  -moz-border-radius: 3px;
  -webkit-border-radius: 3px;
}

.panels-flexible-region .inside {
  padding: 4px;
  margin: 4px;
}

.panels-flexible-region-first .inside {
  margin-left: 0;
}

.panels-flexible-region-last .inside {
  margin-right: 0;
}

/**
 * "Header" minipanel
 */
#mini-panel-header .inside {
  padding: 0;
  margin: 0;
}

#mini-panel-header .pane-block {
  float: right;
}

#mini-panel-header h2.pane-title {
  float: left;
  margin: 0;
  font-weight: normal;
  text-transform: none;
  font-size: 100%;
}

/**
 * The "Stay in touch" copy should be about 7em. We need to set this width 
 * so we can send the h2 left without causing the LIs to wrap.
 */
.pane-menu-menu-stay-in-touch h2.pane-title {
  width: 7em;
  margin-right: -100%;
  margin-left: -7em;
}

.pane-menu-menu-stay-in-touch .pane-content {
  float: left;
}

#mini-panel-header ul.menu {
  padding: 0;
  margin: 0;
}

#mini-panel-header ul.menu li {
  float: left;
  list-style: outside none;
  margin-right: 0.5em;
  margin-left: 0;
}

/**
 * "Footer" minipanel
 */
#mini-panel-footer .inside {
  margin: 0;
  padding: 0;
}

#mini-panel-footer h2.pane-title {
  margin: 0;
  font-size: 100%;
  font-weight: bold;
  text-transform: none;
}

#mini-panel-footer p {
  margin-top: 0;
}

#mini-panel-footer .panels-flexible-region .inside {
  margin: 0;
  padding: 5px;
}

.panels-row-menubar {
  background:  #113b6d;
  border-radius: 5px;
  -moz-border-radius: 5px;
  -webkit-border-radius: 5px;
}

.panels-row-menubar .pane-block ul {
  padding: 0;
  margin: 0;
}

.panels-row-menubar .panels-flexible-region-first .pane-block {
  float: left;
}

.panels-row-menubar .panels-flexible-region-last .pane-block {
  float: right;
}

.panels-row-menubar .pane-block ul.menu li {
  float: left;
  list-style: outside none;
  font-size: 75%;
  padding-top: 0.5em;
}

.pane-menu-secondary-links ul.menu li a /* fake a pipe */ {
  border-right: 1px solid #E2E9E8;
  margin-right: 0.5em;
  padding-right: 0.5em;
}

.pane-menu-secondary-links ul.menu li.last a {
  border-right: 0 none;
}

.panels-row-menubar .pane-block ul.menu li a {
  color: #E2E9E8;
  text-transform: uppercase;
  text-decoration: none;
}

.panels-row-menubar .pane-block ul.menu li a:hover {
  color: #FFF;
}

.panels-row-contentbox {
  background: #f2f2f2;
  border-radius: 3px;
  -moz-border-radius: 3px;
  -webkit-border-radius: 3px;
}

/**
 * "Home" panel page
 */
#panel-page-home .panels-flexible-region-inside {
  background: #e5e5e5;
  height: 250px;
}

#panel-page-home .panels-region-first .panels-flexible-region-inside /* The leftmost region gets a blue background */ {
  background: #e1edf9;
}

#panel-page-home .panels-flexible-row-first .panels-flexible-region-inside {
  height: auto;
  padding: 0;
  margin: 0;
}

/**
 * @file
 * Panels Styling
 */

/**
 * Generic styles
 */ 
div.panel-pane, div.node {
    margin: inherit;
    padding: inherit;
}

.panels-flexible-row .inside {
  padding: 5px;
  margin: 5px;
  border-radius: 3px;
  -moz-border-radius: 3px;
  -webkit-border-radius: 3px;
}

.panels-flexible-row .no-padding .inside {
  padding: 0;
}

.panels-flexible-row .no-margin .inside {
  margin: 0;
}

/**
 * "Header" minipanel
 */
#mini-panel-header .pane-block {
  float: right;
}

#mini-panel-header h2.pane-title {
  float: left;
  margin: 0;
  font-weight: normal;
  text-transform: none;
  font-size: 100%;
}


#mini-panel-header .pane-menu-menu-stay-in-touch ul.menu {
  padding: 0;
  margin: 0;
}

#mini-panel-header .pane-menu-menu-stay-in-touch ul.menu li {
  float: left;
  list-style: outside none;
  margin-right: 1em;
}

/**
 * "Footer" minipanel
 */
.panels-row-menubar {
  background:  #113b6d;
  border-radius: 5px;
  -moz-border-radius: 5px;
  -webkit-border-radius: 5px;
}

.panels-row-menubar .pane-block ul.menu li {
  float: left;
  list-style: outside none;
}

.panels-row-menubar .pane-block ul.menu li a {
  color: #E2E9E8;
}

.panels-row-contentbox {
  background: #f2f2f2;
  border-radius: 3px;
  -moz-border-radius: 3px;
  -webkit-border-radius: 3px;
}

/**
 * "Home" panel page
 */

#panel-page-home .panels-region-first .panels-flexible-region-inside /* The leftmost region gets a blue background */ {
  background: #e1edf9;
  margin-left: 0;
}

#panel-page-home .panels-region-last .panels-flexible-region-inside {
  margin-right: 0;
}

//#panel-page-home .panels-flexible-row-1-main-row .panels-flexible-region-inside /* The top region holds the slideshow and shouldn't get any additional styling */ {
//  margin: 0;
//  padding: 0;
//  background: transparent none;
//  height: auto;
//}

/* Styles for specific content within the "Home" panel page */

#panel-page-home .pane-content ul li {
  list-style-image: url(../images/bullet-square.png);
}

.pane-page-site-name {
  margin: 0;
  font-size: 2em;
  line-height: 1.3em;
  text-transform: uppercase;
  font-weight: normal;
  text-indent: -10000px;
  background: url(../images/bg-site-name.png) 12px 30px no-repeat;
  width: 250px;
  height: 65px;
  display: block;
}

.pane-page-secondary-links ul.links {
  margin: 0;
}
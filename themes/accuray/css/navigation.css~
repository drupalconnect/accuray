/**
 * @file
 * Navigation Styling
 *
 * Default menu styling (ul.menu) is defined in system-menus.css.
 */


/*
 * The active item in a Drupal menu
 */
li a.active {
  color: #000;
}

/*
 * Navigation bar
 */
#navigation {
}

.region-navigation {
}

/*
 * Primary and Secondary menu links
 */
#navigation #main-menu {
  padding-bottom: 0.5em;
  padding-top: 0.5em;
  background: #023064 url(../images/navigation-gradient-tall.png) left top repeat-x;
  border-radius: 5px 5px 0 0;
  -moz-border-radius: 5px 5px 0 0;
  -webkit-border-radius: 5px 5px 0 0;
}

#navigation #main-menu li a {
  color: #e2e9e8;
  text-decoration: none;
  padding-left: 10px;
  /* below should be identical to h2 */
  font-size: 1.1em;
  font-weight: normal;
  line-height: 1.3em;
  margin-top: 0.909em; /* Equivalent to 1em in the page's base font: 1 / 1.1 = 0.909 */
  margin-bottom: 0.909em;
  font-weight: normal;
  text-transform: uppercase;
}

#navigation #main-menu li a:hover {
  color: #FFF;
}

.breadcrumb /* moved here since now it's part of navigation (which it is, semantically, anyway) */ {
  padding-left: 10px;
  background: #023064 url(../images/navigation-gradient-short.png) left top repeat-x;
  border-radius: 0 0 5px 5px;
  -moz-border-radius: 0 0 5px 5px;
  -webkit-border-radius: 0 0 5px 5px;
  color: #e2e9e8;
}

.breadcrumb a {
  font-size: 0.7em;
  color: #e2e9e8;
  text-decoration: none;
  text-transform: uppercase; 
}

.breadcrumb a:hover {
  color: #FFF;
  text-decoration: none;
}

#secondary-menu {
}

.secondary-links {
  margin: 0;
}

/*
 * Menu blocks
 */
.block-menu {
}

/*
 * "Menu block" blocks. See http://drupal.org/project/menu_block
 */
.block-menu_block {
}
